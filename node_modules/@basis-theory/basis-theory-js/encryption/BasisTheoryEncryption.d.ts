import type { Providers } from '../types';
import { EncryptionOptions } from '../types';
import type { EncryptionAdapter, KeyPair } from './types';
export declare class BasisTheoryEncryption implements EncryptionAdapter {
    private readonly adapter;
    constructor(encryptionProvider: Providers);
    init(encryptionOptions: EncryptionOptions): void;
    get name(): string;
    generateKeys(): Promise<KeyPair | string | unknown>;
    encrypt(encryptionKey: string, plainTextData: string): Promise<string>;
    decrypt(decryptionKey: string, cipherTextData: string): Promise<string>;
}
